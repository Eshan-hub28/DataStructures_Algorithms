class Solution {
    public int minStartValue(int[] nums) {
        int sum=0;
        int mini=0;
        for(int i=0;i<nums.length;i++){
            sum+=nums[i];
            mini=Math.min(mini,sum);
        }
        return 1-mini;
    }
}