class Solution(object):
    def getConcatenation(self, nums):
        return nums+nums
        


#2
class Solution:
    def getConcatenation(self, nums):
        n = len(nums) * 2
        a = [0] * n

        for i in range(len(nums)):
            a[i] = nums[i]
            a[i + len(nums)] = nums[i]

        return a